#ifndef __JCHALUPKA_RENAME__
#define __JCHALUPKA_RENAME__

#include "BinaryTree.h"

BinNode * renameNode (BinTree * tree, void * oldName, void * newName);
BinNode * moveNode (BinTree * tree, void * toMove, void * destinationPath);

#endif
